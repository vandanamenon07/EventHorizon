Place the background image you attached here and name it exactly:

  hero-blue.jpg

Path from project root:

  frontend/assets/hero-blue.jpg

Notes:
- The header in `frontend/style.css` references this filename and will use it as the hero background.
- If you want a different filename, update the `background-image: url('./assets/hero-blue.jpg')` line in `frontend/style.css`.
- If you prefer the image embedded, provide the image file and I can add it directly.
